import { initializeApp } from 'firebase/app';import { getAuth,signInWithEmailAndPassword } from 'firebase/auth';import { getFirestore,collection,query,where,getDocs,doc,setDoc,serverTimestamp } from 'firebase/firestore';import { createInterface } from 'node:readline/promises';import { stdin as input,stdout as output } from 'node:process';
const config={apiKey:process.env.VITE_FIREBASE_API_KEY,authDomain:process.env.VITE_FIREBASE_AUTH_DOMAIN,projectId:process.env.VITE_FIREBASE_PROJECT_ID,storageBucket:process.env.VITE_FIREBASE_STORAGE_BUCKET,messagingSenderId:process.env.VITE_FIREBASE_MESSAGING_SENDER_ID,appId:process.env.VITE_FIREBASE_APP_ID};const rl=createInterface({input,output});const email=await rl.question('Admin email: ');const password=await rl.question('Admin password: ');rl.close();const app=initializeApp(config),auth=getAuth(app),db=getFirestore(app);await signInWithEmailAndPassword(auth,email,password);
const products=(await getDocs(collection(db,'products'))).docs.map(x=>({id:x.id,...x.data()}));const find=(type,color)=>products.find(x=>x.type===type&&x.color===color)?.id;const boxes=[['fair-1','Navy Blue Edit','Navy Blue','Beige','Brown',['Fair'],11400],['fair-2','Classic Contrast','Black','Grey','Black',['Fair'],11600],['fair-3','Skyline Edit','Sky Blue','White','Brown',['Fair'],11200],['medium-1','Earthbound Edit','Olive Green','Black','Black',['Medium/Wheatish','Medium / Wheatish'],11800],['medium-2','Warm Maroon Edit','Maroon','Beige','Brown',['Medium/Wheatish','Medium / Wheatish'],11500],['medium-3','Navy Khaki Edit','Navy Blue','Khaki','Brown',['Medium/Wheatish','Medium / Wheatish'],11700],['olive-1','Deep Olive Edit','White','Navy Blue','Black',['Olive'],12000],['olive-2','After Hours','Black','Black','Black',['Olive'],12400],['dark-1','Bright Contrast Edit','White','Navy Blue','Black',['Dark'],11900],['dark-2','Mustard Statement','Mustard','Black','Brown',['Dark'],12100],['dark-3','Light Blue Balance','Sky Blue','Grey','Black',['Dark'],11300]];let added=0;for(const [id,name,shirt,trouser,belt,skinTones,priceCents] of boxes){const shirtId=find('shirt',shirt),trouserId=find('trouser',trouser),beltId=find('belt',belt);if(!shirtId||!trouserId||!beltId){console.log(`Skipped ${id}: missing product for ${shirt}/${trouser}/${belt}`);continue}await setDoc(doc(db,'curatedBoxes',id),{name,description:`A professionally matched 3-piece capsule for ${skinTones.join(' and ')} tones.`,priceCents,shirtId,trouserId,beltId,skinTones,active:true,updatedAt:serverTimestamp()},{merge:true});added++}console.log(`Created or updated ${added} curated boxes.`);
